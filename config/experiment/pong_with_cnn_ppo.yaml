# @package _global_
defaults:
  - override /env: pong_no_frame_skip
  - override /model: ppo
  - override /policy_net: default

model:
  policy: "CnnPolicy"
  batch_size: 128
  learning_rate: 000.1

experiment_name: "pong_with_cnn_poo"

# TODO add wrappers